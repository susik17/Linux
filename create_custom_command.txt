#task1 -> custom command 

1 way -> diretly add to bin folder
susi-> filename | inside file :echo "Hello"
nano susi
chmod +x susi    //chmod-> change mode + -> add permission x -> executable
mv susi /usr/bin


2 way -> custom directory -> add path to .bashrc
mkdir mycmd
cd mycmd
nano susi (write as above)
nano .bashrc

//inside bashrc
export PATH = "$HOME/mycmd:$PATH"

appends the custom command folder to the PATH environment variable - order on above line - 1st check with custom cmds then sys cmds 
$PATH
op:bash: /home/susi-pt8230/mycmd:/home/susi-pt8230/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin:/snap/bin: No such file or directory


//save&reload bashsr
source .bashrc
